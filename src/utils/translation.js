import enData from "../locales/en.json";
import khData from "../locales/kh.json";

export const defaultLang = "en";
export const langs = [{ en: "EN" }, { kh: "KH" }];

export const trans = (key, lang = "en") => {};
